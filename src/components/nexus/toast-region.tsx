"use client"

// Toast region is rendered by shadcn toaster — this is just a placeholder for any
// app-specific toast helpers we may add later. Kept as a marker file so the
// top-level page can compose it without crashing.
export function ToastRegion() {
  return null
}
